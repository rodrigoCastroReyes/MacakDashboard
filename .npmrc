# react-table@7 is unmaintained and still declares a React <=18 peer range, but works
# fine with React 19 (it is hooks-only). Without this, `npm install` fails on ERESOLVE.
# Remove once react-table is replaced by @tanstack/react-table.
legacy-peer-deps=true
